from pathlib import Path
'''
pathlib은 파일 시스템 경로를 단순한 문자열이 아니라 
**객체(Object)**로 다루게 해주는 현대적인 도구입니다. 
슬래시(/)를 사용해 경로를 합치는 등 조작이 매우 직설적이죠.
'''
'''
Path는 엄밀히 말하면 단순한 함수가 아니라, 파이썬의 pathlib 모듈에 정의된 **클래스(Class)**입니다.하지만 사용법은 함수처럼 호출하여 **"운영체제에 맞는 파일 시스템 경로 객체"**를 만드는 역할을 합니다.
1. Path가 필요한 이유 
(문자열과의 차이)예전에는 경로를 다룰 때 일반 문자열("C:\Users\Documents")을 사용했습니다. 하지만 문자열은 운영체제마다 슬래시 방향이 다르고, 경로를 합칠 때마다 귀찮은 처리가 필요했죠.Path를 사용하면 경로 자체가 하나의 똑똑한 객체가 됩니다.
특징설명OS 호환성윈도우(\), 맥/리눅스(/)에 맞춰 슬래시를 자동으로 바꿔줍니다.객체 지향경로 뒤에 .exists(), .mkdir(), .rename() 같은 메서드를 붙여 바로 동작시킵니다.가독성문자열 더하기(+) 대신 / 연산자로 경로를 합칠 수 있어 코드가 깔끔합니다.
2. 주요 기능 및 예시
Path 객체를 생성하면 다음과 같은 유용한 도구들을 사용할 수 있습니다.
경로 병합: Path("my_folder") / "sub_folder" / "file.txt"
파일 존재 확인: path.exists() (파일이나 폴더가 있는지 True/False 반환)
이름 추출: path.name (파일명), path.stem (확장자 제외 이름), path.suffix (확장자)
절대 경로 확인: path.resolve()
[!NOTE]한 줄 정리Path는 파일이나 폴더의 위치를 단순한 글자가 아니라, 조작하기 쉬운 '데이터 덩어리'로 만들어주는 도구입니다.
'''
WORK_DIR = Path(__file__).parent
'''
__file__은 현재 실행 중인 파이썬 파일의 경로를 의미합니다.

.parent는 그 파일이 들어있는 **부모 폴더(디렉토리)**를 가리킵니다.

즉, WORK_DIR은 "이 스크립트가 저장된 폴더"가 됩니다.
'''
OUT_DIR = WORK_DIR / "output"

'''
WORK_DIR 안에 output이라는 이름의 폴더 경로를 생성합니다.
실제 폴더를 만든 것이 아니라 단순히 경로만 만든 것이다. 
그리고 만들어질 때의 이름도 미리 output으로 정해놓았습니다.
pathlib의 매력인 / 연산자를 사용해 경로를 직관적으로 연결하고 있습니다. 
(문자열 더하기보다 훨씬 안전합니다!)
'''
if __name__ == "__main__":
    OUT_DIR.mkdir(exist_ok=True)
'''
if __name__ == "__main__":: 이 파일이 직접 실행될 때만 아래 코드를 실행하라는 뜻입니다 (다른 곳에서 import할 때는 실행되지 않음).

OUT_DIR.mkdir(...): 실제로 폴더를 만드는 명령어입니다.

exist_ok=True: 매우 중요한 옵션입니다! 만약 output 폴더가 이미 존재하더라도 에러를 내지 말고 그냥 넘어가라는 뜻입니다. 이 옵션이 없으면 폴더가 이미 있을 때 프로그램이 멈춰버립니다.
'''

"""
Playwright
Playwright는 마이크로소프트(Microsoft)에서 만든 웹 브라우저 자동화 및 테스트 프레임워크입니다. 쉽게 말해, 사람이 브라우저에서 하는 동작(클릭, 타이핑, 로그인, 페이지 이동 등)을 코드로 대신 실행해 주는 도구입니다.
"""
"""
바로 시작해보는 방법 (Python)
이미 파이참(PyCharm)을 쓰고 계시니, 터미널에서 아래 명령어로 간단히 설치해 볼 수 있습니다.

Bash
pip install playwright
playwright install  
# 필요한 브라우저들을 설치합니다
그다음, 아래 명령어를 실행하면 브라우저가 뜨면서 내가 하는 동작이 파이썬 코드로 실시간 기록됩니다.

Bash
playwright codegen https://www.google.com
혹시 지금 구상 중인 검사 결과 자동 통보 프로그램에 이 기술을 적용해보고 싶으신가요? 간단한 자동 로그인 예제 코드를 짜드릴 수도 있습니다.
"""